feat: Task 1 - multi-tenant backend foundation with RLS and infra setup

- Set up Docker-based infrastructure (PostgreSQL with pgvector + Redis)
- Initialized Alembic migration system for schema versioning
- Designed core multi-tenant schema (tenants, tickets, kb_articles, replies, feedback_events)
- Standardized database identifiers to UUID for distributed consistency
- Enabled PostgreSQL extensions (pgcrypto, vector) for UUID generation and AI embeddings
- Implemented Row Level Security (RLS) for tenant isolation using session-based context (app.tenant_id)
- Added secure RLS policies using BOTH USING and WITH CHECK clauses
- Enforced database-level tenant isolation instead of application-layer filtering
- Fixed critical issues: UUID vs BIGINT mismatch, missing extensions, and superuser RLS bypass behavior
- Established AI-ready foundation with pgvector support for semantic search
- Verified multi-tenant isolation and migration workflow stability

This commit establishes a production-grade, multi-tenant, AI-ready backend foundation with database-enforced security and scalable architecture.